import { Edge, GraphLike } from "../graph";
export declare function dijkstraAll(g: GraphLike, weightFunc: (e: Edge) => number, edgeFunc: (v: string) => Edge[]): Record<string, Record<string, {
    distance: number;
    predecessor?: string;
}>>;
