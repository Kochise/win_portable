##Column layout inside a box
##boxcol column layout corner
##A 'boxcol' is a block similar to 'box', but blocks inside it are laid out in a column, not a row.
box A;
boxcol [line.corner=round] {
    box B;
    box C;
}
A -> B,C;

