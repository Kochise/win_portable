import { XEntry } from './resolve-conflicts';
export interface SortResult {
    vs: string[];
    barycenter?: number;
    weight?: number;
}
export declare function sort(entries: XEntry[], biasRight: boolean): SortResult;
