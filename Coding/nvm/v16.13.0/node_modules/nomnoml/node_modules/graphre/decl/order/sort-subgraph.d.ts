import { SortResult } from "./sort";
import { LayerGraph } from './build-layer-graph';
import { ConstraintGraph } from '../types';
export declare function sortSubgraph(g: LayerGraph, v: string, cg: ConstraintGraph, biasRight: boolean): SortResult;
