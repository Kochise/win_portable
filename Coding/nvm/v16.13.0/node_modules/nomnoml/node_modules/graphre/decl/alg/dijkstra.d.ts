import { Edge, GraphLike } from "../graph";
export interface DjikstraEntry {
    distance: number;
    predecessor?: string;
}
export declare function dijkstra(g: GraphLike, source: string, weightFn: (e: Edge) => number, edgeFn: (v: string) => Edge[]): Record<string, DjikstraEntry>;
