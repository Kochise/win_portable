##Multi-segment arrows
##multi segment arrow join
##Use the join keyword to connect two arrows. This allows to specify different label and attributes for the various segments, but it also allows going the opposite direction.
A, B, C, D;
A->C: First segment;
join C->B;
join B->D: Last segment;
